import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import CalculateJobs from './App';

ReactDOM.render(
  <React.StrictMode>
    <CalculateJobs />
  </React.StrictMode>,
  document.getElementById('root')
);
